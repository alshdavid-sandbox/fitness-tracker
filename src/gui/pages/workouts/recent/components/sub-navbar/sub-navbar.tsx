import './sub-navbar.scss'
import { h } from "preact";

export const SubNavbar = () => (
  <nav className="component-workouts-navbar">
    <div className="active">Recent</div>
    <div>Overview</div>
  </nav>
)