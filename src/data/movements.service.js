import { DB } from '../data/db.service'

export class Movements {
    static getAll() {
        DB.exec(``)
    }

    static add(movement) {
        DB.exec(``)
    }

    static getTypes() {
        DB.exec(``)
    }
}